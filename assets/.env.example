# A股模型实盘对比系统 - 环境变量配置示例
# 使用方法：复制此文件为 .env，并填入实际值

# Tushare API Token（必需）
# 获取方式：访问 https://tushare.pro 注册账号，在"用户中心" -> "接口TOKEN" 复制
TUSHARE_TOKEN=your_tushare_token_here

# 工作空间路径（可选，默认为 /workspace/projects）
COZE_WORKSPACE_PATH=/workspace/projects

# 日志级别（可选，默认为 INFO）
# 可选值：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 最大线程数（可选，默认为 5）
# 用于多线程批量获取数据，根据API限制调整
MAX_WORKERS=5

# API请求超时时间（秒，可选，默认为 30）
REQUEST_TIMEOUT=30

# 缓存过期时间（小时，可选，默认为 24）
CACHE_EXPIRY_HOURS=24
